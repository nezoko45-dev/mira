const express=require("express");const path=require("path");const fs=require("fs");const app=express();const PORT=process.env.PORT||8787;const KEY=process.env.DEEPGRAM_API_KEY;const AGENT=process.env.DEEPGRAM_AGENT_ID;const FAL_KEY=process.env.FAL_KEY;const ROOT=__dirname;app.use(express.static(ROOT));
app.get("/api/config",(q,r)=>AGENT?r.json({agentId:AGENT}):r.status(500).json({error:"Set DEEPGRAM_AGENT_ID on the server."}));
app.get("/api/deepgram-token",async(q,r)=>{if(!KEY)return r.status(500).send("Set DEEPGRAM_API_KEY on the server.");try{const x=await fetch("https://api.deepgram.com/v1/auth/grant",{method:"POST",headers:{Authorization:"Token "+KEY,"Content-Type":"application/json"},body:JSON.stringify({ttl_seconds:300})});const j=await x.json();if(!x.ok)return r.status(x.status).send(j.err_msg||"Deepgram token failed");r.type("text").send(j.access_token)}catch(e){r.status(500).send("Deepgram token service failed")}});
app.get("/api/video/status",(q,r)=>r.json({configured:!!FAL_KEY,provider:"Kling Video 3 via fal"}));
app.post("/api/video",async(q,r)=>{if(!FAL_KEY)return r.status(500).json({error:"Set FAL_KEY on the server first."});try{const{fal}=require("@fal-ai/client");fal.config({credentials:FAL_KEY});const file=path.join(ROOT,"luna mouth closed.png");const b64=fs.readFileSync(file).toString("base64");const dataUri="data:image/png;base64,"+b64;const result=await fal.subscribe("fal-ai/kling-video/v3/standard/image-to-video",{input:{prompt:"Mira gently talks directly to the camera. Natural blinking, subtle head movement, soft breathing, small facial expressions, stable face and identity, gothic romantic dating-app atmosphere, cinematic realistic motion, no camera shake.",start_image_url:dataUri,duration:"5",aspect_ratio:"16:9",generate_audio:false},logs:false});const url=result?.data?.video?.url;if(!url)throw Error("Kling finished without returning a video URL.");r.json({url});}catch(e){r.status(500).json({error:e?.message||"Image-to-video generation failed."})}});
app.get("/{*splat}",(q,r)=>r.sendFile(path.join(ROOT,"index.html")));app.listen(PORT,()=>console.log("Mira: http://localhost:"+PORT));