name: Legacy Mira Companion V3 (manual only)

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: windows-latest
    steps:
      - name: Explain legacy workflow
        shell: pwsh
        run: |
          Write-Host 'The old V3 Chrome/ZIP build is disabled.'
          Write-Host 'Use Build Luna Gothic Companion Electron for the current Electron + local EXE app.'
