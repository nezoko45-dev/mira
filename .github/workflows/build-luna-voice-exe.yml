name: Build Luna Voice EXE

on:
  workflow_dispatch:
  push:
    paths:
      - 'luna-app/server.js'
      - 'luna-app/package.json'
      - 'luna-app/index.html'
      - 'luna mouth closed.png'
      - '.github/workflows/build-luna-voice-exe.yml'

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: luna-app/package.json
      - name: Install dependencies
        working-directory: luna-app
        run: npm install --no-audit --no-fund
      - name: Build Windows EXE
        working-directory: luna-app
        run: npx @yao-pkg/pkg@6.22.0 . --targets node22-win-x64 --output Luna-Backend.exe --compress Brotli
      - name: Verify EXE
        working-directory: luna-app
        run: |
          if (!(Test-Path Luna-Backend.exe)) { throw 'Luna-Backend.exe was not created.' }
      - name: Assemble release
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path release | Out-Null
          Copy-Item luna-app/Luna-Backend.exe release/Luna-Backend.exe
          Copy-Item luna-app/index.html release/index.html
          Copy-Item 'luna mouth closed.png' 'release/luna mouth closed.png'
          Set-Content release/README.txt @('LUNA VOICE APP - FASTER LIVEPORTRAIT','','Double-click Luna-Backend.exe. Chrome opens automatically.','','Pipeline: Microphone -> Deepgram Nova-3 STT -> Luna reply -> Deepgram Aura-2 MP3 -> FasterLivePortrait/JoyVASA audio-driven portrait -> Chrome.','','FasterLivePortrait must be running locally at http://127.0.0.1:9870. Use the official Windows all-in-one FasterLivePortrait package with its audio-driven/JoyVASA support.','','Enter your Deepgram API key in Chrome.','','No MuseTalk. No ffmpeg.wasm. No browser-side MP4 generation. Only luna mouth closed.png is used as the Luna source portrait.')
      - name: Create ZIP
        shell: pwsh
        run: Compress-Archive -Path release/* -DestinationPath Luna-Voice-App.zip -Force
      - uses: actions/upload-artifact@v4
        with:
          name: Luna-Voice-App
          path: Luna-Voice-App.zip
          if-no-files-found: error
